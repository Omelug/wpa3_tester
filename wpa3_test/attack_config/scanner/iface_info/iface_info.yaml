name: iface_info
attacker_module: iface_info
actors:
  scanner:
    source: internal
    selection:
      channel: 6
      condition: [ monitor ]
observers:
  tshark_scanner:
    actor: scanner
    program: tshark
    program_config:
      filter: "special_filter:actors"

attack_config:
  channel: 6