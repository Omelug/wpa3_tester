Dlink_DIR_2150_A1

openwrt
web: root:dlinkroot

    sudo ip addr add 192.168.1.10/24 dev eth0
    sudo ip link set dev eth0 up
    sudo ip route add default via 192.168.1.1 dev eth0 metric 150


### Manual actions (should be done automatically with tester before every test)

After installation:
    Manual time fix:
	/etc/init.d/sysntpd stop
	ntpd -q -n -p 0.openwrt.pool.ntp.org 
	/etc/init.d/sysntpd start

Installation: (can be add with config in ex_WB_programs)
	opkg update
	opkg install tcpdump-mini

	tcpdump -i phy0-ap0 -n

Internet:
kali:
    echo 1 | sudo tee /proc/sys/net/ipv4/ip_forward
    sudo iptables -t nat -A POSTROUTING -o <internet_ip:wlan0> -j MASQUERADE
openwrt:
    uci set network.lan.gateway=<IP_tveho_PC> 192.168.1.188
    uci set network.lan.dns=8.8.8.8
    uci commit network
    /etc/init.d/network restart


firstboot -y && reboot

For EAP need to change wpad version:
    opkg remove wpad-basic-mbedtls
    opkg install wpad-wolfssl