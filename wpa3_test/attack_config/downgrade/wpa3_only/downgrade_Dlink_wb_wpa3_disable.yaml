$extends: ./downgrade_vuln.yaml
name: downgrade_Dlink_wb_wpa3_disable
attacker_module: wpa3_trans_downgrade
actors:
  $DELETE: access_point
  access_point:
    $extends: ../../target/Dlink/Dlink_wb_wpa3_disable.comp.yaml
  client:
    source: internal
    setup:
      program: wpa_supplicant
      program_config:
        ssid: OpenWrt_2_4GHz
        ieee80211w: 2 #WPA3 Disable needs required !!! #TODO add to description/pdf
    $DELETE: selection
  rogue_ap:
    setup:
      program_config:
        version: 2_10
        hostapd-mana_path: config/hostapd-mana_Dlink.conf