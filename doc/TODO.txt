//TODO
-: somthing is broken again, +: new feature, f: fix, r: refactoring,
t: test, d: documentation,  l: logging, ch: change, ?: what?
v: visualization

Version v 0.0.0.0:

t: setup_actor()
+: ClientState observer use in mitm
t?: forgejo compatible github runners for push to main (i s config_validation)

?: works cache correctly with external WB?
v?: get some visualizations from caches
r: extends/$ref - popsat rozdíl do nového souboru yaml_config.md
+: README.md -> rozcestník pro /doc
r: small TODO
+: observer, co se jednou za jako legitimní uživatel čas zkusí připojit

//DONE
t: manual_test to check if all configs in attack_cofigs are valid
r: simplify injection tests
ch: one_iface, two_if test to actual tests, configable tests
ch: change %s style to {} for al errors
t: cache tests
+: TwoIface for injection
+: driver_hash
r: monitor mode hw_capabilies
l: create logging file
+: rozdíl mezi původní a možnou mac
+: interface scan, která ukáře důležité vlastnosti, injection tests
+: add test suite config to wait between tests (is seconds)
ch: change errors.txt logic -> write git hash and info to done.txt
ch: Dos útoky - wrapper pro WPA3-SAE-DoS-Research-Suite
+: ClientState observer
+: observer for connected station on openwrt
r: sniffer loops -> sniffer_helper
rt: change == to CHECK_EQ/CHECK/CHECK_FALSE
rt: test helper function to separated file
r: change == to in REQUIRE, check CHECK/REQUIRE
f: BA/BAR bl0ck attack work only on one mediatek
r: nastavit v clionu styl stringu atd a upravit vše do jednotného stylu - hlavně + ve stringu, namepace (nedosazovat)

//TODO plan to th future
Version v 0.0.0.1
r: sjednotit while loop s logy  Dos útoky


----------------------------- TBD -----------------------------------
Scan:
+: dot11RSNASAEAntiCloggingThreshold, dot11RSNASAERetransPeriod, dot11RSNASAESync
AP_MAX_INACTIVITY (stejné jako v 2022_)
+: ACM activation time (external, internal in hostapd)
f: scan EAP dont work?

Documentation:
+: get LateX from english (or link it form altex git )

Better code:
f: FetchContent_MakeAvailable  paralel downlading should be improved
prerequisities tested versions
ch: change Actor_config variablerto ariables (schovat logiku do toho objektu)
+: přidat spoučtní testů do .run (Clion)
+: better req_error backtracking messages (or table if needed, add debug config)
+: check in setup_chs_attack actors are available
+: simulation interfaces
    // https://github.com/bcopeland/wmediumd
+: add possibility to add different ieee file to hostapd
t?: test with hostapd debugging logs, if are same
ch: change functions to work with HWAdress, not string
r: change all C++ file to .hpp (and change Cmake to accept it)
ch: + docuemnt what exactly are validators/ref/extedns (arent extends and $ref same?)
+: dát  [[nodiscard]] tam, kde je to potřeba
r:  better open/cloose once, multiple ClientStates?

New features:
v?: cache visualization for documnetation?
r: fix whitebox scanners to get more data fir blackboc external
+: create config wizard after first install
+: add option to add actor without interface
f: add network manager off option for actors
+: VIF rozhraní (nejde současně odesílat a příjimat)
+: add sniff_face mon interfaces option in OpennWrtConn
+: extends check for arguemnts
d: SA query
+: get data from mapping/config for --only_stats
+: add SAE forwarding to mitm
+: postupně přepsat DoS wrappers do C++
+: add target to donwload all tools (for all tests)

Fixes:
f: BARS dont work (nejspíš špatně zavhytává správné SSN, ale stejě by v otm případě měloa fungovat jako BAR)
f: openwrt.yaml vygenerovane, zkontrolovat v dokumentaci openwrt

Test cases:
+: test, který by prošel všechny configy testů a vypsal jaké jsou validní
f: all process have to killed before test
t: Actor with internal and AP should accept essid with different essid, but change it
t: některé čipy mohou prej přestat komunikovat po změně mac adresy, zkontrolovat hlavn RTL
+: test_suite - source by test_name

// new attacks?
State Poisoning (2025_SACRED)
Ghost Attack (badtoken pdf)
DPP attacks

Optimization:
?: https://github.com/biojppm/rapidyaml nebo https://github.com/joanvallve/yaml-schema-cpp

PDF:
+: PMKSA
f: deleted_old -> delete_old
+: Donwgrade group https://github.com/Nuseo1/airgeddon_plugins

- Every physical radio can have only one interface in these tests
-> re to to roadmap implement and highlight in documentation
--------------------------------------------------------------------------------------------

TODO
- funguje sudo micro ./ap_debug_test.txt -> inotify
- macchanger musí být až potom / při tom
send_csa_beacon pak nic nedělá