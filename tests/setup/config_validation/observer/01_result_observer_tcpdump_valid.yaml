name: "WPA3-Test"
attacker_module: module1
actors:
  only_actor:
    source: internal
observers:
  observer:
    actor: only_actor
    program: tcpdump
    program_config:
      filter: "wlan.fc.type_subtype == 0x08"