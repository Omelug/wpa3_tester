For compilation #TODO kernel, versions
clang
cmake
make

These programs have to be installed and accessible:

hostapd (if not specified version)
wpa_supplicant (if not specified version)
macchanger
ip (iproute2)
iw

for mitm/fragattacks:

#FIXME in nishaell.nix