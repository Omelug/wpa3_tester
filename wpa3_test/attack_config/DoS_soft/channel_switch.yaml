name: channel_switch_test
wizard_fallback: true

actors:
  target:
    type: AP
    source: internal
    selection:
      condition: [AP]

  attacker:
    type: STA
    source: internal
    selection:
      condition: [monitor]

attack_config:
  validator: ./channel_switch.json
  time_secs: 50
  interval: 10
  new_channel: 1

attack: ../../src/channel_switch
