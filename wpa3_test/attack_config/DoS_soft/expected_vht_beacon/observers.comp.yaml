observers:
  dmesg:
    program: dmesg
  client_mausezahn:
    actor: client
    program: mausezahn
    program_config:
      target_actor: ap
  client_tshark:
    actor: client
    program: tshark
    program_config:
      filter: "special_filter:actors"
  attacker_tshark:
    actor: attacker
    program: tshark
    program_config:
      filter: "special_filter:actors"